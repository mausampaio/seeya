/** Filesystem adapter: whether an arbitrary directory (never `~/.seeya/` itself, `storage/`'s own
 * job) still exists on this machine. See docs/ARQUITETURA.md § "Portas" and V2-T9 item 1. */
export { FsDirectoryExistence } from './directory-existence.js';
