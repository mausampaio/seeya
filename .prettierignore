dist/
# V2-T2: packages/app's own tsc output, deliberately not named "dist" (packages/app/tsconfig.json's
# own comment explains why) — the esbuild bundle at packages/app/dist is already covered by the
# "dist/" pattern above.
dist-tsc/
coverage/
node_modules/

# Authority documents and the work contract: out of scope for automatic reformatting
# (docs/DECISOES.md and docs/ESPECIFICACAO.md can't even be edited by the dev agent — see
# AGENTS.md, the contract CLAUDE.md only imports).
docs/

# AGENTS.md: reformatting only realigns the markdown tables (glossary, language table) to
# padded columns — no wording changes. Every future one-cell edit would then repad the whole
# table, turning a one-line content change into dozens of noise lines in the diff of the
# project's most closely-reviewed document. Churn without gain (S1-T0f).
AGENTS.md

# Worktrees de agentes vivem dentro do repo; sem isto o prettier confere o checkout dos
# outros agentes junto com o nosso (ver o comentario equivalente em eslint.config.js).
.claude/

# backlog/ (D-048): arquivos gerados e mantidos pelo Backlog.md, que usa marcadores de seção próprios
# (<!-- SECTION:... -->) e formato de cabeçalho próprio. Reformatá-los brigaria com a ferramenta.
backlog/
