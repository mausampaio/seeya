node_modules/
dist/
# V2-T2: packages/app's own tsc output — never "dist" (packages/app/tsconfig.json explains why).
dist-tsc/
.claude/worktrees/
.claude/settings.local.json

# Termos que nunca devem ir para o repositório, e as notas que os contêm.
# Ver scripts/verificar-termos-locais.mjs — o guard de pre-commit que usa esta lista.
.termos-locais
/local/
coverage/
*.log
.env
.env.*
.DS_Store
Thumbs.db
.vitest/
*.tsbuildinfo
